packages:
  - packages/*

allowBuilds:
  # eslint-import-resolver-typescript's native binding. Without the build it
  # falls back to a slow path or fails to resolve, so the shared eslint config
  # cannot be tested honestly.
  unrs-resolver: true
